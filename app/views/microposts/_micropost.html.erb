  <p>
  　<%= image_tag micropost.picture.url if micropost.picture? %>
    <%= micropost.content %>    
  </p> 
  Posted <%= time_ago_in_words(micropost.created_at) %> ago.
  <%= link_to "delete", micropost, method: :delete, data: { confirm: "本当に削除しますか？" } %>
  <%= link_to "投稿詳細", micropost_path(micropost) %>
 